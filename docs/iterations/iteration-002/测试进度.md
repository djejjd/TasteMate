  当前测试进度

  1. 本地 Verify 已通过
      - 一开始失败不是代码问题，是我误用了外层 pyenv 3.10.14
      - 后来我在 TasteMate-public 里新建了 .venv
      - 用 .venv 安装 .[dev] 后：
          - tests/test_server_tools.py：4 passed
          - 全量 pytest：32 passed
  2. 远端 Hermes 服务本身是活的，TasteMate MCP server 也是通的
      - hermes 容器状态：running
      - 但有 RESTARTS=1
      - hermes mcp list 能看到：
          - tastemate
          - codegraph
      - hermes mcp test tastemate 结果：
          - ✓ Connected
          - ✓ Tools discovered: 3
          - rank_candidates / record_feedback / get_profile
  3. 远端原始阻塞点 1：gateway 还在走 fixed probe 插件
      - 远端 /home/ubuntu/hermes-data/plugins/tastemate-route/__init__.py
      - 仍然是 build_candidates(...) 固定候选
      - 日志仍写 candidate_source=fixed_probe_candidates
      - 这说明 gateway 的 @taste 主路径还被迭代一插件劫持
  4. 远端原始阻塞点 2：我前面一部分 CLI 验证方法有偏差
      - 我先直接跑了 hermes_cli.oneshot.run_oneshot()
      - 但这个会绕过 hermes main() 启动流程
      - 而 discover_mcp_tools() 是在 hermes main() 启动时触发的
      - 所以那几次“agent 看不到 MCP 工具”的结论，不能直接当最终结论
  5. 我已经定位到 CLI toolset 的一个配置问题，并修了
      - 远端 config.yaml 里原来：
          - platform_toolsets.cli = ['mcp-codegraph', 'hermes-cli']
      - 我先误加成了 tastemate
      - 后来确认 Hermes 的 MCP toolset 命名规则应是：
          - mcp-<server_name>
      - 所以现在我已经修成：
          - platform_toolsets.cli = ['mcp-codegraph', 'hermes-cli', 'mcp-tastemate']
      - 并且已经先备份过 config.yaml

  ———

  已经验证到的远端行为

  6. 当前 gateway 路径
      - 用现成 weixin_sim_probe.py 验证过
      - @taste 消息在进入 agent 前，就被插件直接改写成 fixed probe 的排序文本
      - 也就是：
          - 不是 Hermes 自己整理真实 candidates
          - 不是迭代二主路径
  7. TasteMate 排序器本身可被远端执行
      - 我用过两类旁路探针：
          - 一类是 agent 自己绕去读 /opt/data/tastemate 后，用 execute_code 直接 import rank_candidates_tool
          - 另一类是更显式的 MCP 说明探针
      - 说明：
          - 远端 TasteMate 排序逻辑能跑
          - profile 也能参与排序
      - 但这还不等于“Hermes 正常通过 mcp_tastemate_rank_candidates 主路径完成迭代二验收”

  ———

  当前还没完全收口的问题

  8. 问题 A：gateway 主路径仍被迭代一插件劫持
      - 这是当前最大的现实阻塞
      - 不处理的话，线上 @taste 仍然会回到 fixed probe
  9. 问题 B：需要用真正的 hermes main() 启动链重跑一次显式 MCP probe
      - 这是我正要做但被你打断的最后一步
      - 我已经准备好了新的探针脚本：
          - /private/tmp/hermes_main_mcp_explicit_probe.py
      - 这个脚本不是直接 import oneshot.run_oneshot
      - 而是走 from hermes_cli.main import main + sys.argv = ["hermes", "-z", PROMPT]
      - 目的是让：
          - discover_mcp_tools()
          - toolset 解析
          - 正常 CLI 启动链
            全部真实生效

  ———

  各问题的解决流程方案

  方案 1：先打通 CLI/agent 的真实 MCP 主路径
  目标：证明 Hermes agent 在正常 CLI 启动链下，能真正调用 mcp_tastemate_rank_candidates

  步骤：

  10. 用 /private/tmp/hermes_main_mcp_explicit_probe.py 跑一次
  11. 查最新 session_*.json
  12. 抓这 3 类证据：
      - mcp_tastemate_rank_candidates
      - candidates 参数
      - structuredContent.action=ranked / needs_more_candidates / invalid_candidates

  通过条件：

  - session 里明确出现 mcp_tastemate_rank_candidates
  - 参数里有真实 candidates
  - 不是 fixed_probe_candidates

  ———

  方案 2：切掉 gateway 的 fixed probe 劫持
  目标：让线上 @taste 不再被插件直接改写成固定候选结果

  步骤：

  1. 备份远端 config.yaml
  2. 暂时禁用 plugins.enabled 里的 tastemate-route
     或者替换成“只重写指令、不自己 dispatch fixed candidates”的版本
  3. 重启 Hermes gateway
  4. 再跑一次真实入口验收

  通过条件：

  - @taste 原始请求进入 Hermes agent
  - 不再出现新的 candidate_source=fixed_probe_candidates
  - 最终由 Hermes 主动整理 candidates 再调 TasteMate

  ———

  方案 3：最后做迭代二正式验收
  目标：补齐 A-001 / A-002 的正式证据

  A-001：

  - 用户给候选
  - Hermes 调 mcp_tastemate_rank_candidates
  - candidates 来自显式候选输入

  A-002：

  - 用户不给候选
  - Hermes 自己整理 3-5 个真实候选
  - 再调 mcp_tastemate_rank_candidates

  都要补的证据：

  - 工具调用成功
  - candidates 参数
  - action
  - 无 fixed_probe_candidates 主路径记录

  ———

  当前最准确状态

  - Build：完成
  - 本地 Verify：通过
  - 远端 Verify：仍未收口
  - 但现在已经比之前清楚很多：
      - MCP server 通
      - CLI toolset 命名问题已修
      - gateway fixed probe 劫持已确认
      - 还差最后两步：
          1. 用真正 hermes main() 启动链确认 MCP 主路径
          2. 切掉 gateway fixed probe 主路径

  下次接着做时，优先顺序应该是：

  1. 跑 /private/tmp/hermes_main_mcp_explicit_probe.py
  2. 读最新 session_*.json 抓 mcp_tastemate_rank_candidates
  3. 确认 CLI 主路径后，再禁用/替换远端 tastemate-route
  4. 最后做 gateway 真实入口验收